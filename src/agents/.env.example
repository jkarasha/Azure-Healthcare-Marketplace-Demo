# Azure OpenAI Configuration
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o
# Leave blank to use the agent-framework default. Set only to pin a
# dated Azure OpenAI API version; a value the resource rejects will fail.
AZURE_OPENAI_API_VERSION=

# APIM Base URL (passthrough endpoints - no OAuth required)
APIM_BASE_URL=https://<your-apim-name>.azure-api.net/mcp-pt

# Optional: Override individual MCP server URLs for local development
# MCP_REFERENCE_DATA_URL=http://localhost:7071/mcp
# MCP_CLINICAL_RESEARCH_URL=http://localhost:7072/mcp
# MCP_COSMOS_RAG_URL=http://localhost:7073/mcp

# Optional: Agent model temperature (default 0.3)
# AGENT_TEMPERATURE=0.3
